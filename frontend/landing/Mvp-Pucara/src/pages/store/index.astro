---
import Layout from "../../layouts/Layout.astro";

// Productos de ejemplo (puedes moverlos a un archivo JSON o base de datos después)
const products = [
    {
        id: 1,
        name: "Remera Oficial Pucará",
        description: "Remera oficial de Pucará Gaming con logo bordado de alta calidad",
        price: 100,
        image: "👕",
        category: "Indumentaria",
        stock: 10
    },
    {
        id: 2,
        name: "Gorra Gaming Pro",
        description: "Gorra snapback con logo Pucará y detalles en color naranja",
        price: 100,
        image: "🧢",
        category: "Accesorios",
        stock: 15
    },
    {
        id: 3,
        name: "Mousepad XL Pucará",
        description: "Mousepad gaming de 90x40cm con diseño exclusivo del equipo",
        price: 100,
        image: "🖱️",
        category: "Gaming",
        stock: 20
    },
    {
        id: 4,
        name: "Hoodie Premium",
        description: "Buzo canguro premium con capucha y logo bordado",
        price: 100,
        image: "🧥",
        category: "Indumentaria",
        stock: 8
    },
    {
        id: 5,
        name: "Pack Stickers",
        description: "Pack de 10 stickers con logos y diseños de Pucará Gaming",
        price: 100,
        image: "✨",
        category: "Accesorios",
        stock: 50
    },
    {
        id: 6,
        name: "Botella Térmica",
        description: "Botella térmica de 750ml con logo Pucará, mantiene temperatura 24hs",
        price: 100,
        image: "🧃",
        category: "Accesorios",
        stock: 12
    }
];

// Función helper para formatear precios
const formatPrice = (price: number) => {
    return new Intl.NumberFormat('es-AR', {
        style: 'currency',
        currency: 'ARS',
        minimumFractionDigits: 0
    }).format(price);
};
---

<Layout 
    title="Tienda Oficial - Pucará Gaming"
    description="Tienda oficial de Pucara Gaming. Productos exclusivos: remeras, hoodies, accesorios gaming y merchandising oficial del club."
>
    <main class="font-ubuntu bg-dark text-light min-h-screen pt-24 pb-12" role="main">
        <a href="#main-content" class="skip-link">Saltar al contenido principal</a>
        
        <div id="main-content" class="container mx-auto px-4 max-w-7xl">
            <!-- Header de la tienda -->
            <header class="text-center mb-12" role="banner">
                <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold uppercase tracking-wider mb-4">
                    <span class="bg-gradient-to-r from-primary to-orange-400 bg-clip-text text-transparent">
                        TIENDA OFICIAL
                    </span>
                </h1>
                <p class="text-lg md:text-xl text-light/80 max-w-2xl mx-auto">
                    Productos exclusivos de Pucará Gaming. Pagá con Mercado Pago de forma segura.
                </p>
                
                <!-- Indicador de demo -->
                <div class="mt-6 inline-flex items-center gap-2 bg-primary/20 border border-primary/50 rounded-lg px-4 py-2">
                    <span class="text-primary text-2xl">⚠️</span>
                    <p class="text-sm text-light/90">
                        <strong>Modo Demo:</strong> Los pagos se procesan en modo de prueba
                    </p>
                </div>
            </header>

            <!-- Filtros (opcional para futuro) -->
            <div class="mb-8">
                <div class="flex flex-wrap gap-3 justify-center">
                    <button class="category-filter active px-4 py-2 rounded-lg bg-primary/20 border border-primary text-primary font-semibold transition-all hover:bg-primary hover:text-white" data-category="all">
                        Todos
                    </button>
                    <button class="category-filter px-4 py-2 rounded-lg bg-gray-800/50 border border-gray-700 text-light/70 font-semibold transition-all hover:bg-primary/20 hover:border-primary hover:text-primary" data-category="Indumentaria">
                        Indumentaria
                    </button>
                    <button class="category-filter px-4 py-2 rounded-lg bg-gray-800/50 border border-gray-700 text-light/70 font-semibold transition-all hover:bg-primary/20 hover:border-primary hover:text-primary" data-category="Gaming">
                        Gaming
                    </button>
                    <button class="category-filter px-4 py-2 rounded-lg bg-gray-800/50 border border-gray-700 text-light/70 font-semibold transition-all hover:bg-primary/20 hover:border-primary hover:text-primary" data-category="Accesorios">
                        Accesorios
                    </button>
                </div>
            </div>

            <!-- Grid de productos -->
            <section role="region" aria-labelledby="products-title" class="mb-12">
                <h2 id="products-title" class="sr-only">Catálogo de productos</h2>
                <div id="products-grid" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                    {products.map((product) => (
                        <article 
                            class="product-card bg-dark-subtle border border-gray-800 rounded-lg overflow-hidden transition-all duration-300 hover:border-primary hover:shadow-glow-orange group" 
                            data-category={product.category}
                            role="article"
                        >
                            <!-- Imagen del producto -->
                            <div class="relative bg-gradient-to-br from-gray-800 to-gray-900 h-48 flex items-center justify-center overflow-hidden">
                                <span class="text-6xl group-hover:scale-110 transition-transform duration-300" role="img" aria-label={product.name}>
                                    {product.image}
                                </span>
                                {product.stock < 10 && (
                                    <span class="absolute top-3 right-3 bg-primary text-white text-xs font-bold px-2 py-1 rounded">
                                        ¡Últimas unidades!
                                    </span>
                                )}
                            </div>

                            <!-- Contenido del producto -->
                            <div class="p-5">
                                <div class="mb-2">
                                    <span class="text-xs text-primary/80 uppercase tracking-wider font-semibold">
                                        {product.category}
                                    </span>
                                </div>
                                <h3 class="text-xl font-bold text-light mb-2 group-hover:text-primary transition-colors">
                                    {product.name}
                                </h3>
                                <p class="text-light/70 text-sm mb-4 line-clamp-2">
                                    {product.description}
                                </p>
                                
                                <div class="flex items-center justify-between mb-4">
                                    <div>
                                        <span class="text-2xl font-bold text-primary">
                                            {formatPrice(product.price)}
                                        </span>
                                    </div>
                                    <div class="text-xs text-light/60">
                                        Stock: {product.stock}
                                    </div>
                                </div>

                                <!-- Botón de compra -->
                                <button 
                                    class="buy-button w-full bg-primary hover:bg-primary/80 text-white font-semibold py-3 px-4 rounded-lg transition-all duration-300 hover:scale-105 hover:shadow-glow-orange disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100"
                                    data-product-id={product.id}
                                    data-product-name={product.name}
                                    data-product-price={product.price}
                                    disabled={product.stock === 0}
                                    aria-label={`Comprar ${product.name} por ${formatPrice(product.price)}`}
                                >
                                    {product.stock > 0 ? (
                                        <span class="flex items-center justify-center gap-2">
                                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                                <path d="M9 2a1 1 0 0 0-.894.553L7.382 4H4a1 1 0 0 0-1 1v3a1 1 0 0 0 1 1h16a1 1 0 0 0 1-1V5a1 1 0 0 0-1-1h-3.382l-.724-1.447A1 1 0 0 0 15 2H9zM4 10v9a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-9"/>
                                            </svg>
                                            COMPRAR AHORA
                                        </span>
                                    ) : (
                                        "SIN STOCK"
                                    )}
                                </button>
                            </div>
                        </article>
                    ))}
                </div>
            </section>

            <!-- Información de envío -->
            <section class="bg-dark-subtle border border-primary/20 rounded-lg p-6 mb-8" role="region" aria-labelledby="shipping-info">
                <h2 id="shipping-info" class="text-2xl font-bold text-primary mb-4 flex items-center gap-2">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M18 18.5a1.5 1.5 0 0 1-1.5-1.5a1.5 1.5 0 0 1 1.5-1.5a1.5 1.5 0 0 1 1.5 1.5a1.5 1.5 0 0 1-1.5 1.5m1.5-9l1.96 2.53L17 13.97V9.5M6 18.5a1.5 1.5 0 0 1-1.5-1.5A1.5 1.5 0 0 1 6 15.5A1.5 1.5 0 0 1 7.5 17A1.5 1.5 0 0 1 6 18.5M20 8h-3V4H3c-1.11 0-2 .89-2 2v11h2a3 3 0 0 0 3 3a3 3 0 0 0 3-3h6a3 3 0 0 0 3 3a3 3 0 0 0 3-3h2v-5l-3-4Z"/>
                    </svg>
                    Información de Envío
                </h2>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-4 text-light/80">
                    <div>
                        <h3 class="font-semibold text-light mb-1">🚚 Envío a todo el país</h3>
                        <p class="text-sm">Mediante Correo Argentino y transportes privados</p>
                    </div>
                    <div>
                        <h3 class="font-semibold text-light mb-1">📦 Retiro en local</h3>
                        <p class="text-sm">Gratis en nuestra sede de Buenos Aires</p>
                    </div>
                    <div>
                        <h3 class="font-semibold text-light mb-1">💳 Pago seguro</h3>
                        <p class="text-sm">Procesado por Mercado Pago - Todos los medios de pago</p>
                    </div>
                </div>
            </section>
        </div>

        <!-- Loading overlay -->
        <div id="loading-overlay" class="fixed inset-0 bg-black/80 z-50 items-center justify-center" style="display: none;">
            <div class="text-center">
                <div class="animate-spin rounded-full h-16 w-16 border-t-4 border-b-4 border-primary mx-auto mb-4"></div>
                <p class="text-light text-lg">Procesando tu compra...</p>
            </div>
        </div>
    </main>

    <script>
        // Filtrado de productos
        const filterButtons = document.querySelectorAll('.category-filter');
        const productCards = document.querySelectorAll('.product-card');

        filterButtons.forEach(button => {
            button.addEventListener('click', () => {
                const category = button.getAttribute('data-category');
                
                // Update active button
                filterButtons.forEach(btn => {
                    btn.classList.remove('active', 'bg-primary/20', 'border-primary', 'text-primary');
                    btn.classList.add('bg-gray-800/50', 'border-gray-700', 'text-light/70');
                });
                button.classList.add('active', 'bg-primary/20', 'border-primary', 'text-primary');
                button.classList.remove('bg-gray-800/50', 'border-gray-700', 'text-light/70');

                // Filter products
                productCards.forEach(card => {
                    const productCategory = card.getAttribute('data-category');
                    const htmlCard = card as HTMLElement;
                    if (category === 'all' || productCategory === category) {
                        htmlCard.style.display = 'block';
                        setTimeout(() => {
                            htmlCard.style.opacity = '1';
                            htmlCard.style.transform = 'scale(1)';
                        }, 10);
                    } else {
                        htmlCard.style.opacity = '0';
                        htmlCard.style.transform = 'scale(0.9)';
                        setTimeout(() => {
                            htmlCard.style.display = 'none';
                        }, 300);
                    }
                });
            });
        });

        // Manejo de compras con Mercado Pago
        const buyButtons = document.querySelectorAll('.buy-button');
        const loadingOverlay = document.getElementById('loading-overlay');

        buyButtons.forEach(button => {
            button.addEventListener('click', async () => {
                const productId = button.getAttribute('data-product-id');
                const productName = button.getAttribute('data-product-name');
                const productPrice = button.getAttribute('data-product-price');

                // Mostrar loading
                if (loadingOverlay) {
                    loadingOverlay.style.display = 'flex';
                }

                try {
                    // Llamar al endpoint de creación de preferencia
                    const response = await fetch('/api/create-preference', {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                        },
                        body: JSON.stringify({
                            productId,
                            productName,
                            productPrice: productPrice ? parseFloat(productPrice) : 0
                        })
                    });

                    const data = await response.json();

                    if (data.init_point) {
                        // Redirigir al checkout de Mercado Pago
                        window.location.href = data.init_point;
                    } else {
                        throw new Error('No se pudo crear la preferencia de pago');
                    }
                } catch (error) {
                    console.error('Error al procesar la compra:', error);
                    if (loadingOverlay) {
                        loadingOverlay.style.display = 'none';
                    }
                    alert('Hubo un error al procesar tu compra. Por favor, intenta nuevamente.');
                }
            });
        });

        // Animaciones de entrada
        document.addEventListener('DOMContentLoaded', () => {
            productCards.forEach((card, index) => {
                const htmlCard = card as HTMLElement;
                htmlCard.style.opacity = '0';
                htmlCard.style.transform = 'translateY(20px)';
                setTimeout(() => {
                    htmlCard.style.transition = 'all 0.5s ease';
                    htmlCard.style.opacity = '1';
                    htmlCard.style.transform = 'translateY(0)';
                }, index * 100);
            });
        });
    </script>

    <style>
        .line-clamp-2 {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .skip-link {
            position: absolute;
            left: -9999px;
            z-index: 999;
            padding: 1rem;
            background-color: #ea601a;
            color: white;
            text-decoration: none;
        }

        .skip-link:focus {
            left: 50%;
            transform: translateX(-50%);
            top: 1rem;
        }
    </style>
</Layout>
